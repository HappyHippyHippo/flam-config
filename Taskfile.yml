version: '3'

tasks:
  githooks:
    desc: register the project githooks
    preconditions:
      - test -f ./.githooks/pre-commit
    cmds:
      - git config --local include.path ../.gitconfig  

  default:
    cmds:
      - task --list-all
